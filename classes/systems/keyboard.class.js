/**
 * Holds the current state of keyboard input flags.
 * Each property tracks whether a specific key is currently pressed.
 */
class Keyboard {
    LEFT = false;
    RIGHT = false;
    UP = false;
    SPACE = false;
    ANY = false;
}
